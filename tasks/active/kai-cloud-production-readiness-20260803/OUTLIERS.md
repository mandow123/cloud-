# Outliers

- The direct IP endpoint is HTTP-only. It can be a public fictional-data demo, but cannot be represented as safe for real supplier/customer information.
- The hosted Sites preview is HTTPS and private, but direct-server launch readiness must be assessed independently.
