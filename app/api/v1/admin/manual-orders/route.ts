import { requireManualOrderFlowEnabled } from "@/lib/server/manual-order-feature";import { adminRead } from "../_shared.ts";export const dynamic="force-dynamic";export async function GET(request:Request){return adminRead(request,["FULFILLMENT_READ"],async(store)=>{requireManualOrderFlowEnabled();const records=await store.listAdminManualOrders(100);return{records,count:records.length};});}
