import { assertAccountAuthSameOrigin,AccountAuthError,accountAuthDigest } from "@/lib/server/account-auth";import { getAdminOperationsStore } from "@/lib/server/admin-store";import { apiErrorResponse,beginApiRequest,jsonResponse,readJsonBody,requireIdempotencyKey } from "@/lib/server/api-guard";import { requireTradingAccountSession } from "@/lib/server/entity-ownership";import { requireManualOrderFlowEnabled } from "@/lib/server/manual-order-feature";import { supplyWorkspaceRole } from "@/lib/server/supply-api";
export const dynamic="force-dynamic";export async function POST(request:Request,ctx:{params:Promise<{orderId:string}>}){const c=beginApiRequest(request);try{requireManualOrderFlowEnabled();assertAccountAuthSameOrigin(request);await supplyWorkspaceRole(request,["supplier"]);const a=await requireTradingAccountSession(request);if(!a)throw new AccountAuthError("ACCOUNT_AUTH_REQUIRED",401,"请先登录账户。 ");const input=await readJsonBody(request) as Record<string,unknown>,{orderId}=await ctx.params,result=await(await getAdminOperationsStore()).transitionSupplierManualOrder({principalId:a.account.id,organizationId:a.activeOrganization.id,idempotencyKey:requireIdempotencyKey(request),payloadHash:await accountAuthDigest(JSON.stringify(input))},orderId,"READY",input);return jsonResponse(result,result.replayed?200:201,{"cache-control":"private, no-store","idempotency-replayed":String(result.replayed)},c);}catch(error){return apiErrorResponse(error,undefined,c);}}
